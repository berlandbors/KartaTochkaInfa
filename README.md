# KartaTochkaInfa — Карта Точка Инфа

Веб-приложение для определения геолокации и получения информации о любой точке на карте.

## Возможности

- 📍 **Геопозиция** — широта и долгота с точностью до 4 знаков
- 🏠 **Адрес** — улица, город, район, страна (через OpenStreetMap Nominatim)
- 🏢 **Объект** — тип и название объекта в точке
- 🕐 **Часовой пояс** — зона, UTC-смещение и текущее время (WorldTimeAPI)
- 🔍 **Поиск** — поиск населённых пунктов с автодополнением
- 🔗 **Шаринг** — генерация ссылки и QR-кода для передачи координат
- 📱 **Мобильный интерфейс** — адаптивный дизайн с переключателем Карта/Информация

## Источники данных

| Источник | Назначение |
|---|---|
| [Nominatim (OpenStreetMap)](https://nominatim.openstreetmap.org) | Обратное геокодирование и поиск |
| [WorldTimeAPI](https://worldtimeapi.org) | Часовые пояса |

## Запуск

Для работы ES6-модулей необходим HTTP-сервер (открытие файла напрямую через `file://` не работает):

```bash
# Python 3
python3 -m http.server 8080

# Node.js (npx)
npx serve .

# VS Code
# Установите расширение Live Server и нажмите "Go Live"
```

Затем откройте `http://localhost:8080` в браузере.

## Структура проекта

```
├── index.html              # Основной HTML
├── styles.css              # Кибер-панк стиль
├── js/
│   ├── main.js             # Главный файл, инициализация
│   ├── state.js            # Глобальное состояние
│   └── modules/
│       ├── api/
│       │   ├── nominatim.js    # Геокодинг
│       │   └── worldtime.js    # Часовые пояса
│       ├── ui/
│       │   ├── map.js          # Управление картой
│       │   ├── popup.js        # Попапы маркеров
│       │   ├── modal.js        # Модальные окна
│       │   ├── loading.js      # Индикатор загрузки
│       │   ├── search.js       # Поиск населённых пунктов
│       │   └── share.js        # Шаринг локаций
│       └── utils/
│           ├── cache.js        # Кэширование (15 мин для Nominatim)
│           ├── retry.js        # Повтор запросов с backoff
│           ├── helpers.js      # Вспомогательные функции
│           └── formatters.js   # Форматирование данных
└── README.md
```

## Мобильная поддержка

- **Портрет**: переключатель внизу «Карта / Информация»
- **Ландшафт**: панель информации рядом с картой
- Уведомление с советом использовать горизонтальный режим (с опцией «Не показывать»)
